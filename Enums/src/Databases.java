//ENUM INTRODUCTION
//every enum class extends the Enum class implicitly
public enum Databases {
	// Constants that are objects or references to the class Databases
	// Implicitly they are public, static and final
	/*
	 * Since every Enum class extends the class called Enum from java.lang
	 * 
	 * package. It cannot extend any other classes.
	 */
	MYSQL, ORACLE, SQLSERVER;
}
