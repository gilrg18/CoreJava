
public class Test {
	/*
	 * you can refer to any of the constants inside the Enum using the
	 * 
	 * Enum name because they are all static. public static and final by
	 * default.
	 */
	PaymentType pt = new PaymentType.CREDITCARD();
}
