//TOSTRING METHOD
public class Test {
	public static void main(String[] args){
		Passenger passenger = new Passenger();
		passenger.setFirstName("Gil");
		passenger.setLastName("Rogel");
		//toString method without overriding:
		//getClass().getName()+"@"+Integer.toHexString(hashcode())
		System.out.println(passenger);
		System.out.println(passenger.hashCode());
	}
}
/*
 * It is always a good practice that we override the hashCode method in our classes and return

a unique value for each object.

If we do not override this method, the JVM by default uses the object's address as a hashCode value.
 */
